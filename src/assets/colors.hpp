#ifndef COLORS_HPP
#define COLORS_HPP

#include <SFML/Graphics.hpp>

namespace Colors {
    const sf::Color background(24, 34, 48);
    const sf::Color paddle(52, 94, 106);
    const sf::Color ball(166, 58, 82);
    const sf::Color veryHighLife(234, 108, 129);
    const sf::Color highLife(243, 149, 151);
    const sf::Color middleLife(226, 120, 44);
    const sf::Color lowLife(247, 165, 77);
    const sf::Color veryLowLife(246, 228, 136);
}

#endif